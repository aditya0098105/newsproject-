/**
 * Absolute URL that serves the expose dossier payload as JSON.
 *
 * Replace the placeholder below with the location of the JSON file you
 * maintain (for example, a GitHub Pages URL or a static file on object
 * storage). The app never talks to a CMS anymore—it will only fetch this
 * JSON document and merge it with the fallback content bundled with the
 * app.
 */
export const EXPOSE_CONTENT_URL =
  'https://your-json-source-url.example/expose-content.json';
